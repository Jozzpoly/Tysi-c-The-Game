name: Temporary Interaction Preview One Shot (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "One-shot owner preview trigger disabled after verified deployment."
